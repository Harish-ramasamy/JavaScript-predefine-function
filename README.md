# JavaScript-predefine-function
here I added the Java Script function I manually worked without use the predefine function

so we can know how the predefine functions are working, then it's use to decide the which one we can use for the prefect time.

